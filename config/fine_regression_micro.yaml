run_name: fine_regression_micro_${net.arch}
experiment: RegressionExperiment

backbone: runs/pretraining_micro/2024-07-04_18-06-14
latent_dim: 128

training:
  epochs: 100

net:
  units:
  - ${latent_dim}
  - ${latent_dim}
  - 6
  out_act: sigmoid

data:
  #dir: /scratch/ore/data/x5
  dir: /scratch/nordmann/data_new/x5
  file_by_file: False
  
defaults:
  - default
  - preprocessing: xandy
  - net: mlp
  - _self_